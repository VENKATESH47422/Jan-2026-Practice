- name: TAsk level variables
  hosts: localhost
  tasks:
    - name: play level variables display
      vars:
       movie: salaar from task
       hero: prabhas
      ansible.builtin.debug:
        msg: "I love {{hero}} acted {{movie}} very much"